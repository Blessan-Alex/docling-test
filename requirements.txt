# KMRL Docling Test Suite - Essential Dependencies
# Core document processing
docling>=2.52.0

# Image processing (for OCR testing)
Pillow>=10.0.0

# Optional: For creating test documents (if needed)
python-docx>=1.1.2
openpyxl>=3.1.5

# Optional: For testing (minimal)
pytest>=7.0.0
